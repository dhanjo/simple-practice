{
  "name": "simple-practice",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop simple-practice-api",
    "pm2:restart": "pm2 restart simple-practice-api",
    "pm2:logs": "pm2 logs simple-practice-api",
    "setup": "npx playwright install chromium && mkdir -p logs",
    "test": "npx playwright test",
    "test:reschedule": "npx playwright test appointment-reschedule",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "SimplePractice appointment reschedule automation API",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "tsx": "^4.21.0"
  }
}
